---
to: src/<%= name %>/<%= name %>.spec.ts
---
import { <%= name %> } from './<%= name %>';

describe('<%= name %>', () => {
  it('should work correctly', () => {
    // TODO: Add test cases
  });
});
